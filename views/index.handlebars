<div class="container">
  <div class="row">
    <div class="col" align="center">
      <h1 class="header">Burger App</h1>

    </div>
  </div>
  <div class="row">
    <div class="col text-center">
      <h2>Ordered</h2>
      <ul>
        {{#each burgers}}
        {{#unless devoured}}
        <li>
          {{this.burger_name}}
          <button data-id="{{this.id}}" class="devour-button button">Devour it!</button>
        </li>
        {{/unless}}
        {{/each}}
      </ul>

    </div>
    <div class="col text-center">
      <h2>Eaten</h2>

      <ul>
        {{#each burgers}}
        {{#if devoured}}
        <li>{{this.burger_name}}
          <button data-id="{{this.id}}" class="delete-eaten-button">Delete</button>
        </li>
        {{/if}}
        {{/each}}
      </ul>

    </div>
  </div>
  <div class="row">
    <div class="col" align="center">

      <h2>Make a burger!</h2>
      <form class="create-form">

        <div class="form-group">
          <label for="newBurger">Your Order:</label>
          <input type="text" id="newBurger" name="name">
        </div>
        <button class="add-burger-button button" type="submit">Add Burger</button>
      </form>
    </div>
  </div>

</div>